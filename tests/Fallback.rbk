-- Fallback test file when version/component fails; holds absolutely bare bones Rebekah code

method Main is
begin
    x : Integer = 5
    Char : Character = 'H'

    output(x)

    x := x + 5

    output(x)
end Main
